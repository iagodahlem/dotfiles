for file in $DOTFILES_ZSH/.{exports,aliases,functions}; do
  [ -r "$file" ] && source "$file"
done
unset file

for extension in "$DOTFILES"/extensions/*/zsh/.bootstrap; do
  [ -r "$extension" ] && source "$extension"
done
unset extension

source "$DOTFILES"/asdf/.asdf
source "$DOTFILES"/nvm/.nvm
